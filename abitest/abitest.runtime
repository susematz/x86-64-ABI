# Replace /work/abitest with the path to the abitest directory if necessary

# Create machine
read-configuration abitest.conf

# Set parameters

load-file "/tmp/image.simics" 0x0

source "../scripts/expect.py"
source "../scripts/runtest.py"
